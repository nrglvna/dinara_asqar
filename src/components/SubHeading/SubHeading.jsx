import React from 'react';
const SubHeading = ({title}) => (
  <div style={{marginBottom: '1rem'}} >

    <p className='p__cormorant' style={{color:'var(--color-gray)'}} >{title}</p>
  </div>
);

export default SubHeading;
